
/*
import express from "express";
import {BACKEND_URL} from "@repo/common/config"

const app = express();

app.get("/" , (req,res) : => {
    console.log(BACKEND_URL);
    res.json({
        message : "hello world!!"
    });
});

app.listen(3000);
*/